/**:
  ros__parameters:
    use_sim_time: true # Use simulation time
    joint_names: [arm_j1, arm_j2, arm_j3, arm_j4, arm_j5]
    joint_encoder_invert: [true, false, true, false, false]
    # kp: 500.0
    # kd: 26500.0
    # Optional per-joint overrides (uncomment and set exact lengths if needed)
    kp_gains: [5000.0, 8000.0, 7000.0, 5000.0, 5000.0]
    kd_gains: [500.0, 800.0, 700.0, 500.0, 500.0]
    sim_stop_hold_velocity_epsilon: 0.02
    fk_validation_enabled: true
    fk_validation_offset_xyz: [0.06, -0.235, -2.0]
    fk_validation_log_period_ms: 1000
    
